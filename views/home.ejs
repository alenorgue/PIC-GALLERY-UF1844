<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Picture Gallery</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <%- include('./partials/navbar') %>
    <h1>Lista de Imágenes</h1>
    <ul>
        <% if (Array.isArray(images) && images.length > 0) { images.forEach(function(img) { %>
            <li>
                <img src="<%= img.url %>" alt="<%= img.titulo %>" width="450">
                <p><%= img.titulo %></p>
            </li>
        <% }) } else { %>
            <li>No images found.</li>
        <% } %>
    </ul>

</body>
</html>